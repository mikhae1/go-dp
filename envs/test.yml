---

test:
  defaults:
    branch: test

  local:
    path: /repos

  remote:
    hosts: localhost

    cmd: echo hostname

    log: /var/log/system.log

  targets:
    one:
      remote:
        host: 127.0.0.1
